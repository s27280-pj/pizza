package Pizza;

import java.util.List;

public class PizzaPepperoni extends Pizza {
    public PizzaPepperoni(int id,String name, List<String> components, int spicy, String size, double price) {
        super(id, name, components, spicy, size, price);
    }
}
